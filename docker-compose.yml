version: '3.8'

services:
  portfolio_web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3030:3030"
    environment:
      - PORT=3030
      - HOST=0.0.0.0
    volumes:
      - ./sqlite.db:/app/sqlite.db
      - ./public/uploads:/app/public/uploads
